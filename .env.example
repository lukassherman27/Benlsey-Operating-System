# Bensley Intelligence Platform - Environment Configuration

# Database
DATABASE_PATH=database/bensley_master.db

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# AI Services (Optional - enables natural language queries)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Frontend Configuration (set in frontend/.env.local)
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Development
DEBUG=false

# =============================================================================
# EMAIL SYNC CONFIGURATION
# =============================================================================
# IMAP server settings
EMAIL_SERVER=tmail.bensley.com
EMAIL_PORT=993

# Option 1: Single account (simple)
EMAIL_USERNAME=lukas@bensley.com
EMAIL_PASSWORD=your_password_here

# Option 2: Multiple accounts (JSON array) - takes precedence over single account
# Each account can specify custom folders to sync (defaults to INBOX, Sent)
# Inbox categories are auto-detected based on email address:
#   - projects@  -> 'projects' category (auto-links to projects)
#   - invoices@  -> 'invoices' category (links to invoice tracking)
#   - dailywork@ -> 'internal' category (staff daily updates)
#   - bd@ or businessdevelopment@ -> 'proposals' category
#   - lukas@, bill@ -> 'general' category (personal inboxes)
#
# Example configuration for Phase 1A (projects@ and invoices@):
# EMAIL_ACCOUNTS=[
#   {"email":"lukas@bensley.com","password":"xxx"},
#   {"email":"projects@bensley.com","password":"yyy","folders":["INBOX"]},
#   {"email":"invoices@bensley.com","password":"zzz","folders":["INBOX"]}
# ]

# Safety settings
MAX_EMAILS_PER_RUN=100

# =============================================================================
# OUTBOUND EMAIL CONFIGURATION (for automated reports)
# =============================================================================
# SMTP settings for sending automated emails (e.g., Monday morning report)
# Uses same Bensley mail server as inbound email
#
SMTP_HOST=tmail.bensley.com
SMTP_PORT=465
SMTP_USER=lukas@bensley.com
SMTP_PASSWORD=your_password_here

# Report recipient
REPORT_EMAIL_TO=lukas@bensley.com
